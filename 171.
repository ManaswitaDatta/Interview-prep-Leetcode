class Solution {
    public int titleToNumber(String s) {
        int sum = 0;
        for(char x : s.toCharArray()){
            sum = sum*26 + x - 'A' + 1; 
        }
            
        return sum;
    }
}
