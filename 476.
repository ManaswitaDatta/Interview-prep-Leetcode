class Solution {
    public int findComplement(int num) {
        if(num == 1)
            return 0;
        else
            return (int)(Math.pow(2,Math.floor(Math.log(num)/Math.log(2)) + 1) - 1) - num;
        
    }
}
